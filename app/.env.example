# Laviolette app frontend — environment template
# Copy to app/.env and fill in real values. app/.env is gitignored.
#
# These variables are embedded into the client bundle at build time.
# Safe to ship: the anon key is protected by RLS.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
